#include "main.h"

/**
 * _abs - computes absolute value of integer
 * @n: int to be checked
 *
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
