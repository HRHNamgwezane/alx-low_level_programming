#include "main.h"
#include <stdio.h>

/**
 * _puts - prints string followed by a new line
 * @s: input string
 *
 * Return: 0 (success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
