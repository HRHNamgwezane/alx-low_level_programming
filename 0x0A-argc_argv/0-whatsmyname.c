#include "main.h"
#include <stdio.h>

/**
 * Prgram that prints its name followed by a new line
 *
 * Return: program name
 */
int main(int argc, int char* argv[])
{
	printf("argc = %d\n", argc);
	return (0);
}
